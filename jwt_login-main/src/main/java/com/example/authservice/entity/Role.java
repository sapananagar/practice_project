package com.example.authservice.entity;

/**
 * User roles for Role-Based Access Control (RBAC).
 * Add more roles here as your app grows (e.g., MODERATOR, SUPER_ADMIN).
 */
public enum Role {
    USER,
    ADMIN
}
